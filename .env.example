# Celo Engage Hub - Environment Example
# Rename this file to `.env` and adjust values for your environment.

# Primary RPC endpoint for on-chain reads/writes (Celo Mainnet default).
CELO_ENGAGE_RPC_URL=https://forno.celo.org

# WebSocket endpoint for real-time contract event subscriptions.
CELO_ENGAGE_WS_URL=wss://forno.celo.org/ws

# Backend URL for Self ID verification service.
SELF_BACKEND_URL=http://localhost:8787

# Optional: Talent Protocol username to display the linked profile card.
TALENT_PROTOCOL_USERNAME=

# Port for the Express-based Self verification server.
PORT=8787
